define('editor_tiny/banner_uapa/plugin', ['exports'], function (exports) {
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.default = void 0;

    var configure = function (editor) {
        console.log("TinyMCE UAPA: Configure executed (Standard Path Fix)");

        // Registrar icono
        editor.ui.registry.addIcon('tiny_banner_uapa', '<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><rect x="2" y="4" width="20" height="16" rx="2" stroke="currentColor" stroke-width="2"/><circle cx="12" cy="12" r="3" stroke="currentColor" stroke-width="2"/></svg>');

        var openDataDialog = function () {
            window.alert('¡Funciona! Aquí irá el formulario de Banners.');
        };

        // Registrar botón
        editor.ui.registry.addButton('tiny_banner_uapa', {
            icon: 'tiny_banner_uapa',
            tooltip: 'Insertar Banner UAPA',
            onAction: openDataDialog
        });

        // Registrar menú
        editor.ui.registry.addMenuItem('tiny_banner_uapa', {
            icon: 'tiny_banner_uapa',
            text: 'Insertar Banner UAPA',
            onAction: openDataDialog
        });
    };

    exports.configure = configure;
    return exports;
});
